export enum UserType {
    SELLER = 'seller',
    OWNER = 'owner'
}

export enum HeaderType {
    String = 'string',
    Boolean = 'boolean',
    Number = 'number',
    StringArray = 'stringarray',
}
export enum ControlType {
    Switch = 'switch',
    TextBox = 'textbox',
}